# Shipped as Contents/Resources/settings.toml (bundle-cef-app). Comment-only: built-in defaults apply.
# Override keys only when they should differ; see `resources/settings.default.toml` in the repo.
