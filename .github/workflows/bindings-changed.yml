name: Bindings changed

on:
  push:
    paths:
      - Cargo.toml
      - sys/wrapper.h
      - update-bindings/**
      - .github/workflows/bindings-changed.yml
      - .github/workflows/update-bindings.yml

jobs:
  update-bindings:
    uses: ./.github/workflows/update-bindings.yml
    with:
      branch: "fix/interface-cast"
